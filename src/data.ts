/*
 * ARQUIVO OBSOLETO / DEPRECATED
 *
 * Este arquivo continha produtos mockados (genéricos) que eram usados como fallback.
 * Para garantir que o sistema use APENAS o Supabase em produção, este conteúdo foi removido.
 *
 * Se você precisar dos tipos, importe de './types'.
 */

// import type { Product } from './types';

// export const PRODUCTS: Product[] = [ ... ];
